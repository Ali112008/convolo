# Optional Firebase cloud sync. These client configuration values identify the
# Firebase web app; they are not private credentials. Keep Firebase security
# rules in firebase/firestore.rules enabled to protect learner data.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
